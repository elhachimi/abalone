{
  "name": "abalone",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm --filter database db:deploy && pnpm --filter database db:generate  && pnpm --filter web build",
    "start:web": "pnpm --filter web start",
    "start:admin": "pnpm --filter admin start",
    "dev:web": "pnpm --filter database db:generate && pnpm --filter web dev",
    "dev:admin": "pnpm --filter database db:generate && pnpm --filter admin dev",
    "studio": "pnpm --filter database db:studio",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  }
}
